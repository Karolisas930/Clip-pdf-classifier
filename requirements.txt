--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.36.0
requests>=2.31

torch==2.8.0+cpu
clip @ git+https://github.com/openai/CLIP.git
ftfy>=6.1
regex>=2024.5
tqdm>=4.66

# Only keep these if you actually use OCR/PDF at runtime
pillow>=10.3
numpy>=1.26
pandas>=2.2
PyMuPDF>=1.24
# remove pytesseract if you’re not calling it in the app
# pytesseract>=0.3.10
